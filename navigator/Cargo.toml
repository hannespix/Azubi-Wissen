[workspace]
resolver = "2"
members = ["core", "src-tauri"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "LicenseRef-Intern"
authors = ["RP Freiburg — Ausbildungsberatung"]

[profile.release]
lto = true
codegen-units = 1
strip = true
